let excelPort = require('excel-export');
let fs = require("fs");

let ExcelUtil = {

  write: () => {
      // var datas = req.datas;
      var conf = {};
      var filename = 'filename';  //只支持字母和数字命名
  }
}

module.exports = ExcelUtil;